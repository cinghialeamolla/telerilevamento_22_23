#Qui sotto il codice oggetto di esame

#Per prima cosa, richiamiamo il pacchetti che serviranno per le analisi
library(raster)

#poi faccio il settaggio della cartella di lavoro 
setwd("C:/rexam/")  #questa formula si usa per windows

